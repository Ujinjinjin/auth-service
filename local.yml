version: '3.1'

services:
  
  auth:
    image: ujinjinjin/auth-service:latest
    restart: always
    ports:
      - "5000:80"

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: sa
      POSTGRES_PASSWORD: Qwert1234_
      POSTGRES_DB: auth
    ports:
    - "5432:5432"

  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"
